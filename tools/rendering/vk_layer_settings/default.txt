# Default Khronos validation profile: core checks only.
#
# Selected with FC_VULKAN_VALIDATION_PROFILE=default (fcprobe:
# --validation-profile default), which points VK_LAYER_SETTINGS_PATH here.
# QuarterVulkanWidget only loads VK_LAYER_KHRONOS_validation when
# FC_VULKAN_VALIDATION is set; this file then tunes what the layer reports.
#
# Key names follow the installed validation layer's settings schema.  If a key
# is rejected, run `vkconfig` (or check the layer's vk_layer_settings.txt
# reference) for the current spelling.

khronos_validation.validate_core = true
khronos_validation.report_flags = error,warn
